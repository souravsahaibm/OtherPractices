//Template Literal
let str=`This is a template literal`
console.log(str);

//String Interpolation
const obj={
    name:"Pen",
    Price:10
}
let print=`Price of ${obj.name} is ${obj.Price} rupees.`
console.log(print);

// Array Methods
